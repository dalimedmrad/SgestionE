{% extends 'base.html.twig' %}

{% block title %}Postes{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center">Modifier le Poste N°{{poste.id}}</h1><br>
        <div class="col-md-10">
            {{ include('postes/_form.html.twig') }}
            <a href="{{ path('postes_index') }}" class="btn btn-danger">Annuler</a>
        </div>
    </div>
    
{% endblock %}
