{% extends 'base.html.twig' %}

{% block title %}Poste{% endblock %}

{% block body %}
    <div class="container">
    <h1 class="text-center">Poste N°{{poste.id}}</h1>
    <div class="col-md-10">
        <table class="table table-striped table-hover custab" border="0">
            <tbody>
               
                <tr>
                    <th>Nom</th>
                    <td>{{ poste.nom }}</td>
                </tr>
            </tbody>
        </table>
        <a href="{{ path('postes_index') }}" class="btn btn-secondary">Retourner</a>
    </div>
    </div>
{% endblock %}
